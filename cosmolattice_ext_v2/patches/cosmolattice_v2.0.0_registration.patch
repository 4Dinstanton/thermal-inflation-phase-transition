diff --git a/include/CosmoInterface/evolvers/evolver.h b/include/CosmoInterface/evolvers/evolver.h
index af9da82c..277d229e 100644
--- a/include/CosmoInterface/evolvers/evolver.h
+++ b/include/CosmoInterface/evolvers/evolver.h
@@ -10,6 +10,7 @@
 #include "CosmoInterface/evolvers/leapfrog.h"
 #include "CosmoInterface/evolvers/positionverlet.h"
 #include "CosmoInterface/evolvers/rk2nstorage.h"
+#include "CosmoInterface/evolvers/stochasticrk.h" // >>> thermal-inflation include // <<< thermal-inflation
 #include "CosmoInterface/evolvers/velocityverlet.h"
 
 #include "TempLat/util/exception.h"
@@ -35,6 +36,14 @@ namespace TempLat
     static constexpr bool value = M::IsAxionU1Coupled;
   };
 
+  // >>> thermal-inflation concept
+  template <typename M>
+  concept IsLangevinModel = requires(M &m) {
+    m.stochasticScheme;
+    m.maybeDisableLangevin();
+  };
+  // <<< thermal-inflation
+
   template <class Model> class Evolver
   {
   public:
@@ -56,6 +65,13 @@ namespace TempLat
         rk2n->setDelta(extraFlds);
       }
 
+      // >>> thermal-inflation construct
+      if constexpr (IsLangevinModel<Model>) {
+        if (type == STOCHASTIC) srk = std::make_shared<StochasticLangevin<Model>>(model, rPar);
+      }
+      if (srk != nullptr) return;
+      // <<< thermal-inflation
+
       if (lf == nullptr && vv == nullptr && pv == nullptr && rk2n == nullptr)
         throw(EvolverTypeNotInEvolver("The evolver type you specified was not implemented in the Evolver class, "
                                       "which dispatch between different evolvers. Abort."));
@@ -86,6 +102,14 @@ namespace TempLat
     // @label:gws_evolver_dispatch
     inline void evolve(Model &model, T tMinust0) const
     {
+      // >>> thermal-inflation evolve-dispatch
+      if constexpr (IsLangevinModel<Model>) {
+        if (srk != nullptr) {
+          srk->evolve(model, tMinust0);
+          return;
+        }
+      }
+      // <<< thermal-inflation
       if (model.fldGWs != nullptr && typeGW == LF) {
         lf->kickGWs(model, GWsynced ? 0.5 : 1.0);
         GWsynced = false;
@@ -116,6 +140,14 @@ namespace TempLat
     // @label:gws_evolver_sync
     inline void sync(Model &model, T tMinust0) const
     {
+      // >>> thermal-inflation sync-dispatch
+      if constexpr (IsLangevinModel<Model>) {
+        if (srk != nullptr) {
+          srk->sync(model, tMinust0);
+          return;
+        }
+      }
+      // <<< thermal-inflation
       if (typeGW == LF && !GWsynced) {
         lf->kickGWs(model, 0.5);
         GWsynced = true;
@@ -169,6 +201,7 @@ namespace TempLat
     std::shared_ptr<VelocityVerlet<T>> vv;
     std::shared_ptr<PositionVerlet<T>> pv;
     std::shared_ptr<RK2NStorage<Model>> rk2n;
+    std::shared_ptr<StochasticLangevin<Model>> srk; // >>> thermal-inflation member // <<< thermal-inflation
 
     mutable bool GWsynced;
 
diff --git a/include/CosmoInterface/evolvers/evolvertype.h b/include/CosmoInterface/evolvers/evolvertype.h
index 121e7b40..25122a5f 100644
--- a/include/CosmoInterface/evolvers/evolvertype.h
+++ b/include/CosmoInterface/evolvers/evolvertype.h
@@ -34,7 +34,8 @@ namespace TempLat
    * - RK4_5: 4th order 5 stages low storage Runge-Kutta
    *
    **/
-  enum EvolverType { LF, VV2, VV4, VV6, VV8, VV10, VV6_2, RK2, RK3_3, RK3_4, RK4_5, PV2, PV4, PV6, PV8, PV10, PV6_2 };
+  enum EvolverType { LF, VV2, VV4, VV6, VV8, VV10, VV6_2, RK2, RK3_3, RK3_4, RK4_5, PV2, PV4, PV6, PV8, PV10, PV6_2,
+                     STOCHASTIC /* // >>> thermal-inflation evolver-enum // <<< thermal-inflation */ };
 
   std::istream &operator>>(std::istream &in, EvolverType &eType)
   {
@@ -74,6 +75,8 @@ namespace TempLat
       eType = PV10; // ...order 10
     else if (tmp == "PV6_2" || tmp == "16")
       eType = PV6_2; // alternative scheme for PV6 (see documentation)
+    else if (tmp == "stochastic" || tmp == "STOCHASTIC" || tmp == "stochasticrk")
+      eType = STOCHASTIC; // >>> thermal-inflation evolver-parse // <<< thermal-inflation
     else if (tmp.empty()) {
     } // Otherwise crash for optional parameters.
     else
@@ -117,6 +120,8 @@ namespace TempLat
       return "PV10";
     else if (eType == PV6_2)
       return "PV6_2";
+    else if (eType == STOCHASTIC)
+      return "STOCHASTIC"; // >>> thermal-inflation evolver-string // <<< thermal-inflation
     else
       return "";
   }
diff --git a/include/CosmoInterface/initializers/scalarsingletinitializer.h b/include/CosmoInterface/initializers/scalarsingletinitializer.h
index 9c53537e..a22ad678 100644
--- a/include/CosmoInterface/initializers/scalarsingletinitializer.h
+++ b/include/CosmoInterface/initializers/scalarsingletinitializer.h
@@ -45,8 +45,12 @@ namespace TempLat
 
       auto flagSIC = rPar.SIC;
 
-      if (Model::DefectsModel && (flagSIC != InitialConditionsType::S::DefectsNetwork &&
-                                  flagSIC != InitialConditionsType::S::DefectsWhiteNoise))
+      // >>> thermal-inflation defects-ic
+      constexpr bool skipDefectsICCheck = requires(Model &m) { m.maybeDisableLangevin(); };
+      // <<< thermal-inflation
+      if (!skipDefectsICCheck && Model::DefectsModel &&
+          (flagSIC != InitialConditionsType::S::DefectsNetwork &&
+           flagSIC != InitialConditionsType::S::DefectsWhiteNoise))
         throw(RunParametersInconsistent(
             "You are running a simulation with cosmic defects. Initial conditions specified via the ICtype_S must be "
             "either DefectsNetwork or WhiteNoise. We also recomment using an initial diffusion phase."));
diff --git a/include/CosmoInterface/initializers/scalefactorinitializer.h b/include/CosmoInterface/initializers/scalefactorinitializer.h
index 893c30b4..51d62df3 100644
--- a/include/CosmoInterface/initializers/scalefactorinitializer.h
+++ b/include/CosmoInterface/initializers/scalefactorinitializer.h
@@ -30,10 +30,14 @@ namespace TempLat
     template <class Model, typename T> static void initializeScaleFactor(Model &model, RunParameters<T> &rPar)
     {
       // For model swith defects, only a fixed background is allowed by default
-      if (!rPar.fixedBackground && Model::DefectsModel)
-        throw(RunParametersInconsistent("Running a defects model with self-consistent expansion is not tested, and "
-                                        "features such as (extra)fattening may not work correctly. If you really want "
-                                        "to run this option, comment out this exception in scalefactorinitializar.h"));
+      // >>> thermal-inflation defects-expansion
+      if constexpr (!requires(Model & m) { m.maybeDisableLangevin(); }) {
+        if (!rPar.fixedBackground && Model::DefectsModel)
+          throw(RunParametersInconsistent("Running a defects model with self-consistent expansion is not "
+                                          "tested, and features such as (extra)fattening may not work "
+                                          "correctly."));
+      }
+      // <<< thermal-inflation
 
       // If fixed background, the initial Hubble parameter H0 is given by the user
       if (rPar.fixedBackground) {
diff --git a/source/cosmolattice.cpp b/source/cosmolattice.cpp
index cb00d3fa..20e58855 100644
--- a/source/cosmolattice.cpp
+++ b/source/cosmolattice.cpp
@@ -129,6 +129,15 @@ int main(int argc, char *argv[])
     // 1) We create the class responsible for the initialization
 
     initializer.initialize(model, runParams, measurer.getFilesManager(), extraFlds);
+
+    // >>> thermal-inflation initial-conditions
+    if constexpr (requires { model.applyNumbaInitialConditions(); }) {
+      model.applyNumbaInitialConditions();
+      model.applyUniformPhi();
+      model.applyBubbleSeed();
+      model.freezeInactiveScalars();
+    }
+    // <<< thermal-inflation
     // 2) We initialize the model.
 
     t = runParams.t0;
